#include "main.h"

/**
 * print_alphabelt - print alphabet in lowercase
 */

void print_alphabet(void);
{
	char letter;

	for (letter = 'a'; letter <= 'z'; letter++)
		_putchar(letter);

	_putcahr('\n')
}
